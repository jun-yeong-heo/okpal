% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generators.R
\name{cb_from}
\alias{cb_from}
\title{Generate a colorblind-safe palette from a single base colour}
\usage{
cb_from(
  base,
  n = 8,
  hue_range = 0,
  L_range = c(0.25, 0.95),
  space = c("oklch", "oklab"),
  severity = 1,
  min_dist = 0.05
)
}
\arguments{
\item{base}{Hex colour string to build the palette around.}

\item{n}{Number of colours (default 8).}

\item{hue_range}{Degrees of hue rotation across the palette
(default 0 = monochromatic). Positive values rotate clockwise.}

\item{L_range}{Length-2 numeric vector giving the lightness range
for the palette endpoints (default \code{c(0.25, 0.95)}).}

\item{space}{Interpolation space: \code{"oklch"} (default) or
\code{"oklab"}.}

\item{severity}{CVD severity (0-1, default 1 = full dichromacy).}

\item{min_dist}{Minimum OKLAB distance between any pair after CVD
simulation (default 0.05).}
}
\value{
Character vector of hex colour strings.
}
\description{
Same as \code{\link[=okpal_from]{okpal_from()}} but adjusts the result for colorblind safety
using CVD simulation. If the base colour itself sits on a
problematic hue (e.g. red-green boundary), it is shifted minimally.
}
\examples{
cb_from("#00CC00", n = 6)
cb_from("#3366CC", n = 6, hue_range = 40)
}
