% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generators.R
\name{cb_contrast}
\alias{cb_contrast}
\title{Generate a colorblind-safe palette distant from an existing palette}
\usage{
cb_contrast(
  existing,
  n = NULL,
  L = NULL,
  C = NULL,
  severity = 1,
  min_dist = 0.05
)
}
\arguments{
\item{existing}{Character vector of hex colours to avoid.}

\item{n}{Number of new colours to generate (default same as
\code{length(existing)}).}

\item{L}{Lightness for new colours. If \code{NULL} (default), uses
the median lightness of \code{existing}.}

\item{C}{Chroma for new colours. If \code{NULL} (default), uses the
median chroma of \code{existing}.}

\item{severity}{CVD severity (0-1, default 1).}

\item{min_dist}{Minimum OKLAB distance after CVD simulation
(default 0.05).}
}
\value{
Character vector of hex colour strings.
}
\description{
Same as \code{\link[=okpal_contrast]{okpal_contrast()}} but ensures the new palette is
distinguishable from both the existing palette and itself under
CVD simulation.
}
\section{Limitations}{

Inherits the same shape constraint as \code{\link[=okpal_contrast]{okpal_contrast()}}: candidates
are drawn from a single ring at fixed lightness and chroma. When
\code{existing} is already spread evenly around that ring (e.g.
Dark2-style qualitative palettes, palettes from
\code{\link{cb_safe_palette}} or \code{\link{oklch_qualitative}}),
\code{min_dist} cannot be satisfied for every requested colour. The
function then warns and returns fewer than \code{n} colours.
Sequential / diverging inputs (e.g. viridis) generally succeed.
Use \code{\link{cb_contrast_relaxed}} to widen the search.
}

\examples{
pal_a <- cb_safe_palette(4)
pal_b <- cb_contrast(pal_a, 4)
plot_palette(c(pal_a, pal_b), main = "Original + CB Contrast")
}
\seealso{
\code{\link[=cb_contrast_relaxed]{cb_contrast_relaxed()}}
}
