% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generators.R
\name{cb_contrast_relaxed}
\alias{cb_contrast_relaxed}
\title{Colorblind-safe contrast palette with relaxed L/C}
\usage{
cb_contrast_relaxed(
  existing,
  n = NULL,
  L = NULL,
  C = NULL,
  severity = 1,
  min_dist = 0.05,
  L_tol = 0.1,
  C_tol = 0.05,
  tol_steps = 5L
)
}
\arguments{
\item{existing}{Character vector of hex colours to avoid.}

\item{n}{Number of new colours to generate (default same as
\code{length(existing)}).}

\item{L}{Lightness for new colours. If \code{NULL} (default), uses
the median lightness of \code{existing}.}

\item{C}{Chroma for new colours. If \code{NULL} (default), uses the
median chroma of \code{existing}.}

\item{severity}{CVD severity (0-1, default 1).}

\item{min_dist}{Minimum OKLAB distance after CVD simulation
(default 0.05).}

\item{L_tol}{Maximum lightness deviation from the centre (default 0.1).}

\item{C_tol}{Maximum chroma deviation from the centre (default 0.05).}

\item{tol_steps}{Number of progressive widening steps (default 5).}
}
\value{
Character vector of hex colour strings (length \code{<= n}).
}
\description{
A variant of \code{\link[=cb_contrast]{cb_contrast()}} that widens the L/C search space
progressively when the default single ring cannot satisfy
\code{min_dist} for all \code{n} colours. Returns the first step's
result that places \code{n} colours, or the best partial result
with a warning. Step 1 is identical to \code{cb_contrast}.
}
\seealso{
\code{\link[=cb_contrast]{cb_contrast()}}
}
