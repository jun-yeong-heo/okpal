% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generators.R
\name{okpal_contrast_relaxed}
\alias{okpal_contrast_relaxed}
\title{Generate a contrast palette with relaxed L/C}
\usage{
okpal_contrast_relaxed(
  existing,
  n = NULL,
  L = NULL,
  C = NULL,
  L_tol = 0.1,
  C_tol = 0.05,
  tol_steps = 5L
)
}
\arguments{
\item{existing}{Character vector of hex colours to avoid.}

\item{n}{Number of new colours to generate (default same as
\code{length(existing)}).}

\item{L}{Lightness for new colours. If \code{NULL} (default), uses
the median lightness of \code{existing}.}

\item{C}{Chroma for new colours. If \code{NULL} (default), uses the
median chroma of \code{existing}.}

\item{L_tol}{Maximum lightness deviation from the centre (default 0.1).}

\item{C_tol}{Maximum chroma deviation from the centre (default 0.05).}

\item{tol_steps}{Number of progressive widening steps (default 5).}
}
\value{
Character vector of hex colour strings (length \code{n}).
}
\description{
A variant of \code{\link[=okpal_contrast]{okpal_contrast()}} that widens the L/C search space
progressively when the default single ring cannot fit \code{n}
colours far enough from \code{existing}. Trades a small amount of
visual cohesion with \code{existing} for being able to satisfy
\code{n}. Step 1 is identical to \code{okpal_contrast}; later steps
search a wider grid.
}
\seealso{
\code{\link[=okpal_contrast]{okpal_contrast()}}
}
