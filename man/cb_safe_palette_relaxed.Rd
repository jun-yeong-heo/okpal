% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/colorblind.R
\name{cb_safe_palette_relaxed}
\alias{cb_safe_palette_relaxed}
\title{Generate a colorblind-safe qualitative palette with relaxed L/C}
\usage{
cb_safe_palette_relaxed(
  n = 8,
  L = 0.7,
  C = 0.15,
  severity = 1,
  min_dist = 0.05,
  L_tol = 0.1,
  C_tol = 0.05,
  tol_steps = 5L
)
}
\arguments{
\item{n}{Number of colours (default 8, max ~12 for reliable results).}

\item{L}{Lightness (0-1, default 0.7).}

\item{C}{Chroma (default 0.15).}

\item{severity}{CVD severity (0-1, default 1).}

\item{min_dist}{Minimum OKLAB distance between any simulated pair
(default 0.05).}

\item{L_tol}{Maximum lightness deviation from \code{L} the search
may use (default 0.1, range \code{[0, 1]}).}

\item{C_tol}{Maximum chroma deviation from \code{C} (default 0.05).}

\item{tol_steps}{Number of progressive widening steps (default 5).}
}
\value{
Character vector of hex colour strings (length \code{<= n}).
}
\description{
A variant of \code{\link[=cb_safe_palette]{cb_safe_palette()}} that progressively widens the
search space along lightness and chroma when the default single
OKLCH ring cannot accommodate \code{n} colours. Trades a small
amount of L/C cohesion for being able to satisfy \code{n} when the
strict ring is full.
}
\details{
At step 1 the candidate set is identical to \code{cb_safe_palette}.
If that fails to place \code{n} colours, the search expands to a
grid of L/C points within \code{[L +- L_tol, C +- C_tol]} and tries
again. The grid grows linearly across \code{tol_steps} steps; the
function returns at the first step that satisfies \code{n}, or at
the final step with a warning.
}
\examples{
# Strict variant fails to place all 8
suppressWarnings(length(cb_safe_palette(8)))

# Relaxed variant trades cohesion for completeness
length(cb_safe_palette_relaxed(8, L_tol = 0.15, C_tol = 0.05))
}
\seealso{
\code{\link[=cb_safe_palette]{cb_safe_palette()}}
}
